<!DOCTYPE html>
<html lang="sk">
<head>
  <meta charset="UTF-8" />
  <title><%= title %> | Gym Rezervácie</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
 
  <!-- Favicon -->
  <link rel="icon" type="image/png" href="/images/logo_karta.png" />

  <!-- Bootstrap 5 CSS z CDN (styling framework) -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- CSS -->
  <!-- Naše vlastné štýly -->
  <link rel="stylesheet" href="/css/styles.css" />
  <!-- Font Awesome CDN for icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="d-flex flex-column" style="min-height: 100vh;">
  <!-- Hlavná navigácia + užívateľské info/odhlásenie -->
  <nav class="navbar navbar-dark bg-dark">
    <div class="container-fluid">
      
       <a href="/" class="navbar-brand"><img src="/images/logo.png" alt="Logo" width="200" height="65"></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/">Domov</a></li>
          <li class="nav-item"><a class="nav-link" href="/treneri">Tréneri</a></li>
          <li class="nav-item"><a class="nav-link" href="/treningy">Tréningy</a></li>
          <% if (isAdmin) { %>
            <li class="nav-item"><a class="nav-link" href="/uzivatelia">Používatelia</a></li>
          <% } %>
          <li class="nav-item">
            <a class="nav-link" href="/rezervacie">
              <%= (isAuthenticated && (isAdmin || isTrainer)) ? "Rezervácie" : "Moje rezervácie" %>
            </a>
          </li>
          <% if (isAuthenticated) { %>
            <li class="nav-item dropdown ms-2">
              <a
                class="nav-link dropdown-toggle d-flex align-items-center gap-2"
                href="#"
                role="button"
                data-bs-toggle="dropdown"
                aria-expanded="false"
              >
                <i class="fa-solid fa-user"></i>
                <span><%= (currentUser.username || currentUser.email || "Profil") %></span>
                <% if (currentUser.role && currentUser.role !== "user") { %>
                  <span class="badge bg-info text-dark text-uppercase"><%= currentUser.role %></span>
                <% } %>
              </a>
              <ul class="dropdown-menu dropdown-menu-end">
                <li><a class="dropdown-item" href="/profil"><i class="fa-regular fa-id-card me-2"></i>Môj profil</a></li>
                <li><hr class="dropdown-divider"></li>
                <li>
                  <form action="/logout" method="POST" class="m-0">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <button type="submit" class="dropdown-item text-danger">
                      <i class="fa-solid fa-right-from-bracket me-2"></i>Odhlásiť
                    </button>
                  </form>
                </li>
              </ul>
            </li>
          <% } else { %>
            <li class="nav-item"><a class="nav-link" href="/login">Prihlásiť</a></li>
            <li class="nav-item"><a class="nav-link" href="/register">Registrácia</a></li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>

  <main class="flex-grow-1">
    <div class="container mt-4">
