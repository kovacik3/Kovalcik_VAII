<%- include("partials/header", { title: title }) %>

<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-12 col-md-6">
      <h1 class="mb-4">Upraviť trénera</h1>

      <% if (errors && errors.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <strong>Chyby pri validácii:</strong>
          <ul class="mb-0 mt-2">
            <% errors.forEach(e => { %>
              <li><%= e %></li>
            <% }) %>
          </ul>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% } %>

      <form action="/treneri/<%= trainer.id %>/edit" method="POST" class="js-validate" novalidate>
        <div class="mb-3">
          <label for="name" class="form-label">Meno trénera</label>
          <input
            type="text"
            class="form-control"
            id="name"
            name="name"
            value="<%= trainer.name %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="specialization" class="form-label">Špecializácia</label>
          <input
            type="text"
            class="form-control"
            id="specialization"
            name="specialization"
            value="<%= trainer.specialization %>"
            required
          />
        </div>

        <div class="d-grid gap-2 d-sm-flex justify-content-sm-start">
          <button type="submit" class="btn btn-primary">💾 Uložiť zmeny</button>
          <a href="/treneri" class="btn btn-secondary">❌ Zrušiť</a>
        </div>
      </form>
    </div>
  </div>
</div>

<%- include("partials/footer") %>
